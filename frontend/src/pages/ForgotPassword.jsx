import { IconButton, Box, Card, CardContent, Chip, Divider, FormControl, FormLabel, Input, Typography, Link, Modal, ModalDialog, ModalClose, Textarea, Button, LinearProgress, FormHelperText, Checkbox } from "@mui/joy"
import { useState } from "react";
import { Link as BrowserLink } from "react-router-dom"

import VisibilityIcon from '@mui/icons-material/Visibility';
import VisibilityOffIcon from '@mui/icons-material/VisibilityOff';

// Wenn user email angegeben hat, kann ein code geschickt werden um passwort auf Hallo2025! o.ä. zu setzten (User ändert es dann in den Settings oder direkt per Modal!)
// Ansonsten muss das ganze erstmal wieder durch eine queue bzw per admin mail bestätigt werden (oder eben abgelehnt / per schulcloud gecheckt werden!)
export default function () {
    return (
        <>

        </>
    )
}